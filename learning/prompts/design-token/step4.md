# 变更需求

品牌升级：主色调需要从当前的蓝色系调整为“皇家紫” (#6D28D9)。

# 执行动作

1. 修改 `tokens.json` 中的 `color-brand-primary` 基础值。
2. 重新运行 `npm run build:tokens` 构建脚本。
3. **全局扫描**：检查项目中是否有遗漏的硬编码颜色（例如写死的 `blue-600`），并列出清单建议我替换为 Token。
4. 预览效果：如果可能，生成一个简单的 HTML 预览页展示新旧配色对比。
